# MongoDB Configuration
# Use your Tailscale hostname or IP
MONGO_URI=mongodb://sam-desktop:27017

# Database and collection names
MONGO_DB=cursor_logs
MONGO_COLLECTION=api_requests

# MongoDB connection timeout in milliseconds
MONGO_TIMEOUT_MS=5000

# Production mode (reduces logging verbosity)
# Set to "false" for debugging
PRODUCTION_MODE=true